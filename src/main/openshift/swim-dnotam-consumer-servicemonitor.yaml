apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: swim-dnotam-consumer-metrics
  labels:
    app: swim-dnotam-consumer
    app.kubernetes.io/name: swim-dnotam-consumer
    app.kubernetes.io/part-of: swim-operator
spec:
  selector:
    matchLabels:
      app: swim-dnotam-consumer
  endpoints:
  - port: http
    path: /q/metrics
    interval: 30s
    scrapeTimeout: 10s
